/*jshint esversion: 6 */

// npm install gulp-modernizr --save-dev
import '../../temp/scripts/modernizr';

//npm install picturefill // now responsive images will work with all browser's
import 'picturefill';

//npm install lazysizes / go to webpack and add an entry point
import 'lazysizes';
